# Copy this file to backend/.env and paste your real key. NEVER commit backend/.env.
# Get a key at https://aistudio.google.com/apikey
# If this is unset, the /insight endpoint still works using a SHAP/rule-based fallback.
GEMINI_API_KEY=your_key_here
